import { ReactNode } from 'react';

export interface IconBorderFrameProps {
  size: number;
  svgSize?: number;
  before?: ReactNode;
  layout?: ReactNode;
  className?: string;
  backgroundColor?: string;
}
